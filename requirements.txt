# Gunakan versi yang sudah teruji stabil di Streamlit Cloud
streamlit>=1.28.0,<2.0.0
requests>=2.28.0

# Hapus package yang bermasalah - gunakan alternatif:
# Untuk SSL check - sudah built-in, tidak perlu install
# Untuk DNS - opsional, bisa di-skip dulu
